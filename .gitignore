# Logs (Keep logs ignored)
logs
*.log

# WhatsApp & Session Cache
**/.wwebjs_cache/
**/.wwebjs_auth/
**/whatsapp-session/
**/_IGNORE_*
_IGNORE_studio-whatsapp/
# WhatsApp Code (Sessions are still ignored below)
# whatsapp-server/ (Removed this line to allow code upload)

# Dependencies (Allowed for full upload as requested)
# node_modules/
# **/node_modules/

# Build outputs (Allowed for full upload)
# dist/
# dist-ssr/
# build/

# Env & Secrets (Keep .env ignored for safety unless asked)
.env
.env.local
*.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
